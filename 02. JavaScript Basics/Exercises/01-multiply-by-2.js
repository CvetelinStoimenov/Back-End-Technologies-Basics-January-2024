// Define a function named multiplyByTwo that takes a parameter 'num'
function multiplyByTwo(num) {
    // Enable strict mode to catch common coding errors and improve performance
    'use strict';

    // Log the result of multiplying 'num' by 2 to the console
    console.log(num * 2);
}

// Call the multiplyByTwo function with an argument of 2
multiplyByTwo(2);
